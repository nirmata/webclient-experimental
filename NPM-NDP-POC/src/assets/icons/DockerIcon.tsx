import Docker from '../../../assets/docker.svg';

export function DockerIcon() {
  return (
    <div style={{ marginLeft: '-6px!important' }}>
      <img src={Docker} width={21} height={21} />
    </div>
  );
}
