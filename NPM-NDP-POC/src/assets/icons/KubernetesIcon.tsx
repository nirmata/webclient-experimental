import Kubernetes from '../../../assets/kubernetes.svg';

export function KubernetesIcon() {
  return (
    <div style={{ marginLeft: '-6px!important' }}>
      <img src={Kubernetes} width={21} height={21} />
    </div>
  );
}
